diff --git a/node_modules/@raphckrman/react-native-alarm-kit/ios/AlarmKit.swift b/node_modules/@raphckrman/react-native-alarm-kit/ios/AlarmKit.swift
index 6b487fd..be4f2ce 100644
--- a/node_modules/@raphckrman/react-native-alarm-kit/ios/AlarmKit.swift
+++ b/node_modules/@raphckrman/react-native-alarm-kit/ios/AlarmKit.swift
@@ -1,4 +1,6 @@
+#if canImport(AlarmKit)
 import AlarmKit
+#endif
 import SwiftUI
 import NitroModules
 
@@ -6,6 +8,7 @@ class AlarmKit: HybridAlarmKitSpec {
     @available(iOS 15.1, *)
     public func requestAlarmPermission() throws -> NitroModules.Promise<Bool> {
         return NitroModules.Promise.async {
+            #if canImport(AlarmKit)
             if #available(iOS 26.0, *) {
                 let manager = AlarmManager.shared
                 let state: AlarmManager.AuthorizationState
@@ -23,12 +26,20 @@ class AlarmKit: HybridAlarmKitSpec {
                     userInfo: [NSLocalizedDescriptionKey: "AlarmKit requires iOS 26.0 or later"]
                 )
             }
+            #else
+            throw NSError(
+                domain: "AlarmKitError",
+                code: 1,
+                userInfo: [NSLocalizedDescriptionKey: "AlarmKit framework not available in this SDK"]
+            )
+            #endif
         }
     }
 
     @available(iOS 15.1, *)
     public func scheduleFixedAlarm(title: String, stopBtn: CustomizableAlarmButton, tintColor: String, secondaryBtn: CustomizableAlarmButton?, timestamp: Double?, countdown: AlarmCountdown?) throws -> NitroModules.Promise<Bool> {
         return NitroModules.Promise.async {
+            #if canImport(AlarmKit)
             if #available(iOS 26.0, *) {
                 let manager = AlarmManager.shared
 
@@ -94,6 +105,13 @@ class AlarmKit: HybridAlarmKitSpec {
                     userInfo: [NSLocalizedDescriptionKey: "AlarmKit requires iOS 26.0 or later"]
                 )
             }
+            #else
+            throw NSError(
+                domain: "AlarmKitError",
+                code: 1,
+                userInfo: [NSLocalizedDescriptionKey: "AlarmKit framework not available in this SDK"]
+            )
+            #endif
         }
     }
 
@@ -109,6 +127,7 @@ class AlarmKit: HybridAlarmKitSpec {
         countdown: AlarmCountdown?
     ) throws -> NitroModules.Promise<Bool> {
         return NitroModules.Promise.async {
+            #if canImport(AlarmKit)
             if #available(iOS 26.0, *) {
                 let manager = AlarmManager.shared
 
@@ -184,6 +203,13 @@ class AlarmKit: HybridAlarmKitSpec {
                     userInfo: [NSLocalizedDescriptionKey: "AlarmKit requires iOS 26.0 or later"]
                 )
             }
+            #else
+            throw NSError(
+                domain: "AlarmKitError",
+                code: 1,
+                userInfo: [NSLocalizedDescriptionKey: "AlarmKit framework not available in this SDK"]
+            )
+            #endif
         }
     }
 }
