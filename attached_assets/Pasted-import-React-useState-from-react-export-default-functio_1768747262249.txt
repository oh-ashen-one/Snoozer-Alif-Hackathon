import React, { useState } from 'react';

export default function LiveActivityA2() {
  const [state, setState] = useState('sleeping');

  return (
    <div className="container">
      <style>{styles}</style>

      {/* Toggle */}
      <div className="toggle-wrap">
        <button 
          className={`toggle-btn ${state === 'sleeping' ? 'active' : ''}`}
          onClick={() => setState('sleeping')}
        >
          😴 Sleeping
        </button>
        <button 
          className={`toggle-btn ${state === 'ringing' ? 'active' : ''}`}
          onClick={() => setState('ringing')}
        >
          🔔 Ringing
        </button>
      </div>

      {/* Phone */}
      <div className="phone">
        <div className="phone-screen">
          
          {/* Dynamic Island */}
          <div className={`island ${state === 'ringing' ? 'glow-red' : ''}`}>
            <span className={`island-icon ${state === 'ringing' ? 'shake' : ''}`}>{state === 'sleeping' ? '⏰' : '🔔'}</span>
            <span className={`island-text ${state === 'ringing' ? 'red' : ''}`}>{state === 'sleeping' ? '6:00' : 'WAKE'}</span>
            <div className="island-div" />
            <span className={`island-stake ${state === 'ringing' ? 'red' : ''}`}>{state === 'sleeping' ? '$5' : '-$5'}</span>
          </div>

          {/* Lock Screen Time */}
          <div className="lock-time-wrap">
            <div className="lock-time">{state === 'sleeping' ? '11:43' : '6:00'}</div>
            <div className="lock-date">Sunday, January 18</div>
          </div>

          {/* Live Activity */}
          <div className="widget-area">
            {state === 'sleeping' ? (
              <div className="la">
                <div className="la-top">
                  <span className="la-time">6:17:42</span>
                  <span className="la-label">until alarm</span>
                </div>
                <div className="la-cards">
                  <div className="la-card">
                    <span className="la-emoji">👴</span>
                    <span className="la-text">Text wife's dad</span>
                  </div>
                  <div className="la-card">
                    <span className="la-emoji">💔</span>
                    <span className="la-text">Text ex "i miss u"</span>
                  </div>
                  <div className="la-card">
                    <span className="la-emoji">📧</span>
                    <span className="la-text">Email your boss</span>
                  </div>
                </div>
              </div>
            ) : (
              <div className="la la-ring">
                <div className="la-alert">
                  <span className="shake">🔔</span>
                  <span className="la-warn">GET UP OR ELSE:</span>
                </div>
                <div className="la-cards-ring">
                  <div className="la-card-ring">
                    <span className="la-emoji">👴</span>
                    <span className="la-text-ring">Wife's dad gets "hey bro what are you wearing"</span>
                  </div>
                  <div className="la-card-ring">
                    <span className="la-emoji">💔</span>
                    <span className="la-text-ring">Ex gets "i miss u"</span>
                  </div>
                  <div className="la-card-ring">
                    <span className="la-emoji">📧</span>
                    <span className="la-text-ring">Boss gets "running late again"</span>
                  </div>
                </div>
                <button className="la-btn">✓ I'M UP</button>
              </div>
            )}
          </div>

          <div className="home-bar" />
        </div>
      </div>
    </div>
  );
}

const styles = `
  * { box-sizing: border-box; }
  
  .container {
    min-height: 100vh;
    background: #080808;
    font-family: -apple-system, BlinkMacSystemFont, sans-serif;
    padding: 24px;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 24px;
  }

  .toggle-wrap {
    display: flex;
    gap: 6px;
  }
  .toggle-btn {
    padding: 10px 16px;
    background: #1a1a1a;
    border: none;
    border-radius: 10px;
    font-size: 14px;
    font-weight: 600;
    color: #666;
    cursor: pointer;
  }
  .toggle-btn.active {
    background: #FB923C;
    color: #000;
  }

  .phone {
    width: 300px;
    height: 650px;
    background: #000;
    border-radius: 48px;
    padding: 10px;
    box-shadow: 0 25px 50px rgba(0,0,0,0.6);
  }
  .phone-screen {
    width: 100%;
    height: 100%;
    background: linear-gradient(180deg, #1a1a2e 0%, #0f0f1a 100%);
    border-radius: 40px;
    position: relative;
    overflow: hidden;
  }
  .home-bar {
    position: absolute;
    bottom: 8px;
    left: 50%;
    transform: translateX(-50%);
    width: 110px;
    height: 4px;
    background: #fff;
    border-radius: 2px;
    opacity: 0.2;
  }

  /* Dynamic Island */
  .island {
    position: absolute;
    top: 12px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    align-items: center;
    height: 34px;
    min-width: 140px;
    background: #000;
    border-radius: 18px;
    padding: 0 6px;
    gap: 6px;
  }
  .island-icon { font-size: 14px; margin-left: 4px; }
  .island-text { font-size: 13px; font-weight: 700; color: #fff; }
  .island-div { width: 1px; height: 16px; background: rgba(255,255,255,0.2); }
  .island-stake { font-size: 12px; font-weight: 700; color: #22C55E; margin-right: 6px; }
  .red { color: #EF4444 !important; }
  .glow-red { box-shadow: 0 0 20px rgba(239,68,68,0.5); }

  /* Lock Screen */
  .lock-time-wrap {
    position: absolute;
    top: 70px;
    left: 50%;
    transform: translateX(-50%);
    text-align: center;
  }
  .lock-time {
    font-size: 72px;
    font-weight: 200;
    color: #fff;
    letter-spacing: -2px;
  }
  .lock-date {
    font-size: 16px;
    color: rgba(255,255,255,0.5);
    margin-top: -6px;
  }

  /* Widget Area */
  .widget-area {
    position: absolute;
    top: 220px;
    left: 14px;
    right: 14px;
  }

  /* Live Activity */
  .la {
    background: rgba(25, 25, 30, 0.95);
    border-radius: 22px;
    backdrop-filter: blur(20px);
    border: 1px solid rgba(255,255,255,0.06);
    padding: 16px;
  }
  .la-ring {
    border: 1px solid rgba(239,68,68,0.4);
    box-shadow: 0 0 40px rgba(239,68,68,0.2);
  }
  .la-top {
    text-align: center;
    margin-bottom: 14px;
  }
  .la-time {
    display: block;
    font-size: 36px;
    font-weight: 700;
    color: #fff;
    font-variant-numeric: tabular-nums;
  }
  .la-label {
    font-size: 12px;
    color: rgba(255,255,255,0.4);
  }
  .la-cards {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .la-card {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 10px 12px;
    background: rgba(255,255,255,0.05);
    border-radius: 12px;
  }
  .la-emoji {
    font-size: 22px;
  }
  .la-text {
    font-size: 14px;
    color: rgba(255,255,255,0.75);
  }
  .la-alert {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 12px;
    font-size: 22px;
  }
  .la-warn {
    font-size: 17px;
    font-weight: 800;
    color: #EF4444;
  }
  .la-cards-ring {
    display: flex;
    flex-direction: column;
    gap: 8px;
    margin-bottom: 14px;
  }
  .la-card-ring {
    display: flex;
    align-items: flex-start;
    gap: 10px;
    padding: 10px;
    background: rgba(239,68,68,0.1);
    border-radius: 10px;
  }
  .la-text-ring {
    font-size: 13px;
    color: rgba(255,255,255,0.85);
    line-height: 1.3;
  }
  .la-btn {
    width: 100%;
    padding: 14px;
    background: #22C55E;
    border: none;
    border-radius: 14px;
    font-size: 16px;
    font-weight: 800;
    color: #fff;
    cursor: pointer;
  }

  /* Animations */
  .shake {
    animation: shake 0.25s ease-in-out infinite;
  }
  @keyframes shake {
    0%, 100% { transform: rotate(0); }
    25% { transform: rotate(-12deg); }
    75% { transform: rotate(12deg); }
  }
`;
```

---

**😴 Sleeping:**
```
┌─────────────────────────────────────┐
│           6:17:42                   │
│          until alarm                │
│                                     │
│ ┌───────────────────────────────┐   │
│ │ 👴  Text wife's dad           │   │
│ └───────────────────────────────┘   │
│ ┌───────────────────────────────┐   │
│ │ 💔  Text ex "i miss u"        │   │
│ └───────────────────────────────┘   │
│ ┌───────────────────────────────┐   │
│ │ 📧  Email your boss           │   │
│ └───────────────────────────────┘   │
└─────────────────────────────────────┘
```

**🔔 Ringing:**
```
┌─────────────────────────────────────┐
│ 🔔 GET UP OR ELSE:                  │
│                                     │
│ ┌───────────────────────────────┐   │
│ │ 👴 Wife's dad gets "hey bro.."│   │
│ └───────────────────────────────┘   │
│ ┌───────────────────────────────┐   │
│ │ 💔 Ex gets "i miss u"         │   │
│ └───────────────────────────────┘   │
│ ┌───────────────────────────────┐   │
│ │ 📧 Boss gets "running late.." │   │
│ └───────────────────────────────┘   │
│                                     │
│         [✓ I'M UP]                  │
└─────────────────────────────────────┘